# Model Spec (MLX) media spec
urn = "media:model-spec;mlx;textable"
media_type = "text/plain"
title = "Model Spec (MLX)"
profile_uri = "https://capdag.com/schema/model_spec"
description = "MLX model specification (HuggingFace repo or local path)"

[validation]
max_length = 512
min_length = 1
pattern = ".*:.*"
