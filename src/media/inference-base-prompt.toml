# inference-base-prompt.toml
# Generated from key_reference.csv

urn = "media:inference-base-prompt;setting;textable"
media_type = "text/plain"
title = "Base prompt for the model"
profile_uri = "https://capdag.com/schema/inference-base-prompt"
description = "Base prompt for the model"

[metadata]
category = "inference"
subcategory = "inference_generation"
display_index = 2
ui_type = "SETTING_UI_TYPE_TEXT_AREA"
data_type = "SETTING_DATA_TYPE_STRING"
default = "You are MachineFabric, a helpful assistant. For greetings, general knowledge, or conversational messages, respond directly without using tools. ONLY use tools when the user explicitly asks about documents in their library. Never fabricate document content. Never simulate multi-turn conversations."
overridable_by = ["cap", "model", "user", "end_user"]
