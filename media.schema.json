{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://capdag.com/schema/media.schema.json",
  "title": "Media Definition",
  "description": "Schema for media def TOML/JSON definitions in the CAPDAG registry",
  "type": "object",
  "required": ["urn", "media_type", "title"],
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 0,
      "description": "Per-definition version. Optional; absent ⇒ 0. v0 is the frozen pre-versioning state at the flat R2 path. v ≥ 1 is published at media/<sha256>/<version>.json. Bumped explicitly in source TOMLs when content changes."
    },
    "urn": {
      "type": "string",
      "pattern": "^media:",
      "description": "The media URN identifier (e.g., 'media:pdf;binary')"
    },
    "media_type": {
      "type": "string",
      "pattern": "^[a-z]+/[a-z0-9+._-]+$",
      "description": "MIME media type (e.g., 'application/pdf', 'text/plain')"
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Display-friendly title for the media type"
    },
    "profile_uri": {
      "type": "string",
      "pattern": "^https?://",
      "description": "Profile URI for schema reference"
    },
    "schema": {
      "type": "object",
      "description": "Optional JSON Schema for validation"
    },
    "description": {
      "type": "string",
      "description": "Optional short plain-text description of the media type"
    },
    "documentation": {
      "type": "string",
      "description": "Optional long-form markdown documentation. Rendered in media info panels, the cap navigator, capdag-dot-com, and anywhere else a rich-text explanation of the media def is useful. In TOML sources, author this as a triple-quoted literal string (''') so newlines and markdown punctuation pass through unchanged; the JSON generator re-escapes newlines per JSON rules on output."
    },
    "validation": {
      "type": "object",
      "description": "Validation rules for values of this media type",
      "additionalProperties": false,
      "properties": {
        "min": {
          "type": "number",
          "description": "Minimum numeric value"
        },
        "max": {
          "type": "number",
          "description": "Maximum numeric value"
        },
        "min_length": {
          "type": "integer",
          "description": "Minimum string/array length",
          "minimum": 0
        },
        "max_length": {
          "type": "integer",
          "description": "Maximum string/array length",
          "minimum": 0
        },
        "pattern": {
          "type": "string",
          "description": "Regular expression pattern for validation"
        },
        "allowed_values": {
          "type": "array",
          "description": "List of allowed values",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "metadata": {
      "type": "object",
      "description": "Optional metadata for display and categorization (arbitrary key-value pairs)",
      "additionalProperties": true
    },
    "extensions": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9+]+$"
      },
      "description": "File extensions for storing this media type (e.g., ['pdf'], ['jpg', 'jpeg']). Should not include the leading dot."
    }
  },
  "additionalProperties": false
}
