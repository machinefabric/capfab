{
  "urn": "cap:candle;generate-image-embeddings;in=\"media:image;png\";ml-model;out=\"media:embedding-vector;record;textable\"",
  "command": "generate_image_embeddings",
  "title": "Generate Image Embeddings (Candle) (Candle (Rust safetensors), ML Model)",
  "cap_description": "Generate image embeddings using CLIP models with Candle ML framework",
  "documentation": "## Purpose\n\nEncodes a PNG image into a dense floating-point vector using CLIP (Contrastive Language-Image Pretraining). The resulting vector lives in the same semantic space as CLIP text embeddings, enabling cross-modal similarity search — find images by text queries and vice versa.\n\n## Inputs\n\n- A PNG image file.\n- A CLIP model spec from HuggingFace. Defaults to `clip-vit-base-patch32` (512 dimensions).\n\n## Outputs\n\nA JSON record containing the image embedding vector and model metadata.\n\n## Notes\n\n- CLIP image and text vectors are compatible: you can compare an image embedding to text embeddings generated by a CLIP text encoder in the same vector space.\n- Input must be PNG. Convert other image formats upstream.\n- For describing image content in natural language rather than embedding it, use [Describe Image (Candle)](https://capdag.com/browse/cap:candle;in=\"media:image;png\";ml-model;op=describe_image;out=\"media:image-description;textable\").\n\n## Backend\nThe inference framework or runtime that loads and executes a model. Backend choice\ndetermines weight format, hardware acceleration, and runtime characteristics. A media\nURN that omits the backend tag is backend-agnostic — it accepts any backend.\n\nCandle is a lightweight Rust-based ML framework. Loads models in safetensors format\ndirectly on CPU, Metal, or CUDA. Embeddable in Rust applications without Python\nruntime overhead.\n\n## ML model\nMarker indicating the cap invokes a machine-learning model. Inputs typically\ninclude a model spec or model artifact; the cap is subject to model-resolution\nand hardware-pressure policies.\n\nThe cap invokes a machine-learning model.",
  "metadata": {},
  "args": [
    {
      "media_urn": "media:file-path;textable",
      "required": true,
      "sources": [
        {
          "stdin": "media:image;png"
        },
        {
          "position": 0
        }
      ],
      "arg_description": "Path to the image file to embed"
    },
    {
      "media_urn": "media:model-spec;candle;image-embeddings;textable",
      "required": false,
      "sources": [
        {
          "cli_flag": "--model"
        }
      ],
      "arg_description": "CLIP model name from HuggingFace",
      "default_value": "hf:openai/clip-vit-base-patch32?include=*.json,*.safetensors,pytorch_model.bin"
    }
  ],
  "output": {
    "media_urn": "media:embedding-vector;textable;record",
    "output_description": "Image embedding vector with model metadata"
  }
}