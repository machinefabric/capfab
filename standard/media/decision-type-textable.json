{
  "urn": "media:decision-type;textable",
  "title": "Decision Type (Textable)",
  "media_type": "text/plain",
  "profile_uri": "https://capdag.com/schema/decision_type",
  "description": "Decision Type value",
  "documentation": "## Purpose\nSpecifies the evaluation strategy a decision machine should apply. The value controls how the model frames and returns its judgment.\n\n## When to use\nSet this parameter on any machine that routes between multiple decision modes. Allowed values are `binary`, `classification`, `reasoning`, `verification`, and `comparison`. If omitted, machines typically default to `binary`.\n\n## Notes\n`binary` returns a true/false answer; `classification` selects from a label set; `reasoning` includes a rationale; `verification` checks a claim; `comparison` ranks two or more items. Pair with [Decision](https://capdag.com/browse/media:decision;json;record;textable) for the output format.\n\n## Textable\nCoercion marker indicating the value is faithfully representable as UTF-8 text.\nStrings are textable trivially; integers and booleans are textable via their\nstring form; structured values are textable when JSON-serialised. Binary blobs\n(images, audio, raw bytes) are NOT textable — converting them to text requires\nexplicit transformation that may be lossy.\n\nThe value is faithfully representable as UTF-8 text without loss.",
  "validation": {
    "allowed_values": [
      "binary",
      "classification",
      "reasoning",
      "verification",
      "comparison"
    ]
  }
}