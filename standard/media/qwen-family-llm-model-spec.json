{
  "urn": "media:llm;model-spec;qwen;textable",
  "media_type": "text/plain",
  "title": "Qwen Family LLM Model Spec",
  "profile_uri": "https://capdag.com/schema/model_spec",
  "description": "Model specification for Qwen-family instruct models. Uses ChatML im_start/im_end template.",
  "documentation": "## Purpose\nTags a model spec as belonging to Alibaba's Qwen model family, enabling the runtime to apply the ChatML `<|im_start|>` / `<|im_end|>` template used by Qwen2 and related models.\n\n## When to use\nThis tag is assigned during content inspection when a Qwen-family model is detected. The Qwen family also covers multimodal variants (Qwen-VL); use the vision-specific spec types for those.\n\n## Notes\nQwen models use the same ChatML template as some other model families. The runtime should verify the exact template from the model's tokenizer_config.json to avoid collisions.\n",
  "extensions": [
    "txt"
  ],
  "validation": {
    "max_length": 512,
    "min_length": 1,
    "pattern": ".*:.*"
  }
}