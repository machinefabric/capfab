{
  "urn": "media:model-spec;gguf;textable;embeddings",
  "media_type": "text/plain",
  "title": "GGUF Embeddings Model Spec",
  "profile_uri": "https://capdag.com/schema/model_spec",
  "description": "GGUF model specification for embedding vector generation.",
  "documentation": "## Purpose\nIdentifies a GGUF-format embedding model used to produce dense vector representations of text via the llama.cpp backend. These models are optimised for similarity search and retrieval rather than text generation.\n\n## When to use\nUse this type when wiring an embedding cap that targets the GGUF backend. The default points to nomic-embed-text-v1.5, a high-quality open embedding model. Pair with [Embedding Chunk Size](https://capdag.com/browse/media:embedding-chunk-size;search;limit;operator;task;textable;numeric) and [Embedding Chunk Overlap](https://capdag.com/browse/media:embedding-chunk-overlap;search;limit;operator;task;textable;numeric) parameters.\n\n## Notes\nEmbedding GGUF models are loaded differently from LLM models and should not be used for text generation. The model's output dimension is reported in [LLM Model Info](https://capdag.com/browse/media:llm-model-info;json;record) and can be confirmed via [Model Dimensionality](https://capdag.com/browse/media:model-dim;integer;textable;numeric).\n",
  "extensions": [
    "txt"
  ],
  "metadata": {
    "default": "hf:nomic-ai/nomic-embed-text-v1.5-GGUF"
  },
  "validation": {
    "max_length": 512,
    "min_length": 1,
    "pattern": ".*:.*"
  }
}