{
  "urn": "media:env;textable;record",
  "media_type": "text/plain",
  "title": "Environment File",
  "profile_uri": "https://capdag.com/schema/env",
  "description": "Environment variables configuration",
  "documentation": "## Purpose\nCarries `.env` files that define environment variable key-value pairs for local development, CI pipelines, and containerized deployments. The format is simple: one `KEY=value` pair per line, with optional comments.\n\n## When to use\nUse `media:env;textable;record` when a pipeline node reads, generates, or validates environment configuration — for example, a secret injection step that merges a `.env` file into a container environment, or a linting step that checks for required variables.\n\n## Notes\n`.env` files frequently contain sensitive credentials and should never be committed to version control. Tools like `dotenv`, `direnv`, and Docker Compose consume this format. Variable interpolation and multi-line values are supported inconsistently across parsers; check the target parser's documentation. See also [Environment File](https://capdag.com/browse/media:env;textable;record).\n",
  "extensions": [
    "env"
  ]
}