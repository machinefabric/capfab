{
  "urn": "cap:embeddings-dimensions;gguf;in=\"media:embeddings;gguf;model-spec;textable\";ml-model;out=\"media:integer;model-dim;numeric;textable\"",
  "command": "get_embedding_dimensions",
  "title": "Get Embeddings Dimensions (GGUF) (GGUF (llama.cpp), ML Model)",
  "cap_description": "Query the dimensionality of embeddings for a given GGUF model",
  "documentation": "## Purpose\n\nReports the vector size that a GGUF embedding model produces by reading `n_embd` from the model's metadata. Use this before creating a vector index so you know how many dimensions to allocate.\n\n## Inputs\n\n- A GGUF model spec (HuggingFace repo or local path). Defaults to `nomic-embed-text-v1.5-GGUF`.\n\n## Outputs\n\nA single integer: the `n_embd` value from the GGUF model header.\n\n## Notes\n\n- Reads model metadata only — no inference is run.\n- Use at pipeline setup time to parameterize downstream vector store caps.\n- See also: [Generate Vector Embeddings (GGUF)](https://capdag.com/browse/cap:gguf;in=\"media:textable\";ml-model;op=generate_embeddings;out=\"media:embedding-vector;textable;record\")\n\n## Backend\nThe inference framework or runtime that loads and executes a model. Backend choice\ndetermines weight format, hardware acceleration, and runtime characteristics. A media\nURN that omits the backend tag is backend-agnostic — it accepts any backend.\n\nGGUF is the llama.cpp container format. Quantised weights packaged into a single\nfile with embedded metadata. Runs efficiently on CPU and via Metal/CUDA acceleration.\nStandard format for general-purpose LLM and embedding inference across hardware.\n\n## ML model\nMarker indicating the cap invokes a machine-learning model. Inputs typically\ninclude a model spec or model artifact; the cap is subject to model-resolution\nand hardware-pressure policies.\n\nThe cap invokes a machine-learning model.",
  "metadata": {},
  "args": [
    {
      "media_urn": "media:model-spec;gguf;textable;embeddings",
      "required": false,
      "sources": [
        {
          "cli_flag": "--model-spec"
        },
        {
          "stdin": "media:model-spec;gguf;textable;embeddings"
        }
      ],
      "arg_description": "GGUF model specification to query dimensions for",
      "default_value": "hf:nomic-ai/nomic-embed-text-v1.5-GGUF"
    }
  ],
  "output": {
    "media_urn": "media:model-dim;integer;textable;numeric",
    "output_description": "Dimensionality of the embeddings (n_embd from model metadata)"
  }
}