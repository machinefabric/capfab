{
  "urn": "cap:in=media:textable;ml-model;mlx;op=generate_embeddings;out=\"media:embedding-vector;record;textable\"",
  "command": "generate_embeddings",
  "title": "Generate Vector Embeddings (MLX)",
  "cap_description": "Generate vector embeddings from text using MLX-based models on Apple Silicon",
  "documentation": "## Purpose\n\nEncodes text into dense floating-point vectors using MLX-optimized embedding models on Apple Silicon. Delivers fast, energy-efficient embedding on Mac M-series hardware. Long documents are automatically chunked.\n\n## Inputs\n\n- A model spec (HuggingFace repo or local path). Defaults to `all-MiniLM-L6-v2-4bit` (384 dimensions, 4-bit quantized).\n- A text file to embed.\n- Chunk size in words (default 400) and chunk overlap in words (default 50).\n\n## Outputs\n\nA JSON record containing an array of embedding objects with chunk text, position, embedding vector, and model metadata.\n\n## Notes\n\n- Requires Apple Silicon (M1 or later). Not available on Intel Macs or non-Apple hardware.\n- MLX uses the unified memory architecture; large models share CPU/GPU memory without copying.\n- Use [Get Embeddings Dimensions (MLX)](https://capdag.com/browse/cap:in=\"media:model-spec;mlx;textable;embeddings\";ml-model;mlx;op=embeddings_dimensions;out=\"media:model-dim;integer;textable;numeric\") to check the vector size.\n- For cross-platform pipelines, use [Generate Vector Embeddings (Candle)](https://capdag.com/browse/cap:candle;in=\"media:textable\";ml-model;op=generate_embeddings;out=\"media:embedding-vector;textable;record\") or [Generate Vector Embeddings (GGUF)](https://capdag.com/browse/cap:gguf;in=\"media:textable\";ml-model;op=generate_embeddings;out=\"media:embedding-vector;textable;record\") instead.\n",
  "metadata": {},
  "args": [
    {
      "media_urn": "media:model-spec;mlx;textable;embeddings",
      "required": false,
      "sources": [
        {
          "cli_flag": "--model"
        }
      ],
      "arg_description": "Path to MLX embedding model directory",
      "default_value": "hf:mlx-community/all-MiniLM-L6-v2-4bit"
    },
    {
      "media_urn": "media:file-path;textable",
      "required": true,
      "sources": [
        {
          "stdin": "media:textable"
        },
        {
          "position": 0
        }
      ],
      "arg_description": "Path to text file to generate embeddings from"
    },
    {
      "media_urn": "media:chunk-size;textable;numeric",
      "required": false,
      "sources": [
        {
          "cli_flag": "--chunk-size"
        }
      ],
      "arg_description": "Chunk size in words for splitting input text",
      "default_value": 400
    },
    {
      "media_urn": "media:chunk-overlap;textable;numeric",
      "required": false,
      "sources": [
        {
          "cli_flag": "--chunk-overlap"
        }
      ],
      "arg_description": "Chunk overlap in words when splitting input text",
      "default_value": 50
    }
  ],
  "output": {
    "media_urn": "media:embedding-vector;textable;record",
    "output_description": "JSON containing embeddings, chunk metadata and model information"
  }
}