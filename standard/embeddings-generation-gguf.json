{
  "urn": "cap:gguf;in=media:textable;ml-model;op=generate_embeddings;out=\"media:embedding-vector;record;textable\"",
  "command": "generate_embeddings",
  "title": "Generate Vector Embeddings (GGUF)",
  "cap_description": "Generate vector embeddings from text using GGUF models with llama.cpp backend",
  "documentation": "## Purpose\n\nEncodes text into dense floating-point vectors using quantized GGUF embedding models via llama.cpp. Suitable for semantic search, clustering, and RAG. Long documents are automatically split into overlapping chunks.\n\n## Inputs\n\n- A GGUF model spec (HuggingFace repo or local path). Defaults to `nomic-embed-text-v1.5-GGUF`.\n- A text file to embed.\n- Chunk size in words (default 400), chunk overlap in words (default 50), and batch size in tokens (default 2048).\n\n## Outputs\n\nA JSON record containing an array of embedding objects. Each object includes the chunk text, its position, the L2-normalized embedding vector, and model metadata.\n\n## Notes\n\n- GGUF quantization makes models smaller and faster at a small accuracy trade-off. Q4 and Q8 quantizations are common choices.\n- Vectors are L2-normalized. Use cosine similarity or dot product when comparing vectors from this cap.\n- Batch size must be large enough to accommodate the tokenized chunk. Increase if you see truncation errors.\n- Use [Get Embeddings Dimensions (GGUF)](https://capdag.com/browse/cap:gguf;in=\"media:model-spec;gguf;textable;embeddings\";ml-model;op=embeddings_dimensions;out=\"media:model-dim;integer;textable;numeric\") to check the vector size.\n- For Apple Silicon, prefer [Generate Vector Embeddings (MLX)](https://capdag.com/browse/cap:mlx;in=\"media:textable\";ml-model;op=generate_embeddings;out=\"media:embedding-vector;textable;record\").\n",
  "metadata": {},
  "args": [
    {
      "media_urn": "media:model-spec;gguf;textable;embeddings",
      "required": true,
      "sources": [
        {
          "cli_flag": "--model-spec"
        }
      ],
      "arg_description": "GGUF model specification (HuggingFace repo or local path)",
      "default_value": "hf:nomic-ai/nomic-embed-text-v1.5-GGUF"
    },
    {
      "media_urn": "media:file-path;textable",
      "required": true,
      "sources": [
        {
          "stdin": "media:textable"
        },
        {
          "position": 0
        }
      ],
      "arg_description": "Path to text file to generate embeddings from"
    },
    {
      "media_urn": "media:chunk-size;textable;numeric",
      "required": false,
      "sources": [
        {
          "cli_flag": "--chunk-size"
        }
      ],
      "arg_description": "Chunk size in words for splitting input text",
      "default_value": 400
    },
    {
      "media_urn": "media:chunk-overlap;textable;numeric",
      "required": false,
      "sources": [
        {
          "cli_flag": "--chunk-overlap"
        }
      ],
      "arg_description": "Chunk overlap in words when splitting input text",
      "default_value": 50
    },
    {
      "media_urn": "media:batch-size;inference;limit;operator;task;textable;numeric",
      "required": false,
      "sources": [
        {
          "cli_flag": "--batch-size"
        }
      ],
      "arg_description": "Token batch size for model inference (must accommodate tokenized chunk)",
      "default_value": 2048
    }
  ],
  "output": {
    "media_urn": "media:embedding-vector;textable;record",
    "output_description": "JSON containing L2-normalized embeddings, chunk metadata and model information"
  }
}