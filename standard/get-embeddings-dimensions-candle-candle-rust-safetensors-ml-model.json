{
  "urn": "cap:candle;embeddings-dimensions;in=\"media:candle;embeddings;model-spec;textable\";ml-model;out=\"media:integer;model-dim;numeric;textable\"",
  "command": "get_embedding_dimensions",
  "title": "Get Embeddings Dimensions (Candle) (Candle (Rust safetensors), ML Model)",
  "cap_description": "Query the dimensionality of embeddings for a given Candle model",
  "documentation": "## Purpose\n\nReports the vector size that a Candle-based embedding model produces. Use this before creating a vector index so you know how many dimensions to allocate.\n\n## Inputs\n\n- A model spec pointing to a HuggingFace embedding model. Defaults to `all-MiniLM-L6-v2` (384 dimensions).\n\n## Outputs\n\nA single integer: the number of dimensions in each embedding vector this model produces.\n\n## Notes\n\n- The result is read from the model's saved configuration, not from running inference, so it is fast and requires no GPU memory.\n- Use at pipeline setup time to parameterize downstream vector store caps.\n- See also: [Generate Vector Embeddings (Candle)](https://capdag.com/browse/cap:candle;in=\"media:textable\";ml-model;op=generate_embeddings;out=\"media:embedding-vector;textable;record\")\n\n## Backend\nThe inference framework or runtime that loads and executes a model. Backend choice\ndetermines weight format, hardware acceleration, and runtime characteristics. A media\nURN that omits the backend tag is backend-agnostic — it accepts any backend.\n\nCandle is a lightweight Rust-based ML framework. Loads models in safetensors format\ndirectly on CPU, Metal, or CUDA. Embeddable in Rust applications without Python\nruntime overhead.\n\n## ML model\nMarker indicating the cap invokes a machine-learning model. Inputs typically\ninclude a model spec or model artifact; the cap is subject to model-resolution\nand hardware-pressure policies.\n\nThe cap invokes a machine-learning model.",
  "metadata": {},
  "args": [
    {
      "media_urn": "media:model-spec;candle;textable;embeddings",
      "required": false,
      "sources": [
        {
          "cli_flag": "--model"
        },
        {
          "stdin": "media:model-spec;candle;textable;embeddings"
        }
      ],
      "arg_description": "HuggingFace model name to query dimensions for",
      "default_value": "hf:sentence-transformers/all-MiniLM-L6-v2"
    }
  ],
  "output": {
    "media_urn": "media:model-dim;integer;textable;numeric",
    "output_description": "Dimensionality of the embeddings (positive integer)"
  }
}