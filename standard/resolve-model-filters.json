{
  "urn": "cap:in=\"media:model-spec;textable\";op=resolve-model-filters;out=\"media:json;model-filter-resolution;record;textable\"",
  "command": "resolve",
  "title": "Resolve Model Filters",
  "cap_description": "Dry-run a model download to preview which files would be fetched, including any architecture-driven additions on top of the user's filters.",
  "documentation": "## Purpose\n\nResolves a model spec against the remote repository without downloading anything, returning the full effective file list. The response distinguishes the user's include/exclude filter result from the additions modelcartridge would make based on the detected architecture (`config.json` `model_type`).\n\nThis is the dry-run companion of [Download Model](https://capdag.com/browse/cap:in=\"media:model-spec;textable\";op=download-model;out=\"media:download-result;textable;record\"). Use it to drive read-only previews in download wizards, where the user's filter is shown alongside the modelcartridge-added mandatory files (e.g. `tokenizer_config.json`, `tokenizer.model`, `special_tokens_map.json`) that are required to load that architecture.\n\n## Inputs\n\n- A model spec string with optional include/exclude filters (e.g. `hf:org/model?include=*.safetensors,*.json&exclude=*-fp32*`).\n- Optional HuggingFace bearer token (`hf_token`) for gated repositories.\n\n## Outputs\n\nA JSON record:\n\n```\n{\n  \"model_spec\": \"hf:org/model?...\",\n  \"applied\": {\n    \"include\": [\"*.safetensors\", \"*.json\"],\n    \"exclude\": [\"*-fp32*\"],\n    \"mandatory_added\": [\"tokenizer_config.json\", \"tokenizer.model\"],\n    \"model_type\": \"mistral\",\n    \"effective_files\": [...]\n  }\n}\n```\n\n## Notes\n\n- The resolution result is advisory — the actual download response includes its own `applied_filters` field reflecting the file set used at download time. Compare both to detect repository changes between resolution and download.\n- The `mandatory_added` list is empty when the user's filter already covers everything the architecture needs.\n- For gated repositories without a valid `hf_token`, the call fails hard with a clear authentication error.\n",
  "metadata": {},
  "args": [
    {
      "media_urn": "media:model-spec;textable",
      "required": true,
      "sources": [
        {
          "stdin": "media:model-spec;textable"
        },
        {
          "position": 0
        }
      ],
      "arg_description": "Model spec to resolve filters for"
    },
    {
      "media_urn": "media:hf-token;secret;textable",
      "required": false,
      "sources": [
        {
          "cli_flag": "--hf-token"
        }
      ],
      "arg_description": "HuggingFace bearer token. Required when targeting a gated repository."
    }
  ],
  "output": {
    "media_urn": "media:model-filter-resolution;json;record;textable",
    "output_description": "JSON record describing the architecture-aware effective file list, including any modelcartridge additions on top of the user's filter."
  }
}